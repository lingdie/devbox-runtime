# Sandbox Framework Example

This template provides a multi-runtime sandbox environment.

## Included Toolchain

- Bun (latest)
- Node.js 22
- Python 3.14

## How to Run

- Development: `bash entrypoint.sh`
- Production: `bash entrypoint.sh production`

The default service listens on port `8080` and returns runtime version details in JSON format.
